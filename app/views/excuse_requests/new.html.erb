<div class="card">
  <h2>今日は何から逃げたいですか？</h2>
  <p>
    タスクを入力すると、AI（風）が全力で逃げ道を探します。
  </p>

  <%= form_with model: @excuse_request, local: true do |f| %>

    <div class="form-group">
      <%= f.label :task_name, "やりたくないタスク" %>
      <%= f.text_field :task_name %>
    </div>

    <div class="form-group">
      <%= f.label :urgency, "緊急度" %>
      <%= f.select :urgency,
        [["低", "low"], ["中", "mid"], ["高", "high"]],
        {} %>
    </div>

    <div class="form-group">
      <%= f.label :mood, "今日の気分（0〜100）" %>
      <%= f.number_field :mood, min: 0, max: 100 %>
    </div>

    <div class="form-group text-right">
      <%= f.submit "逃げ道を提案する", class: "btn-primary" %>
    </div>

  <% end %>
</div>

<div class="text-center">
  <%= link_to "過去の言い訳を見る", excuse_requests_path, class: "btn btn-secondary" %>
</div>
